/**
 * Placeholder — scroll nativo, sem hijack.
 * Three.js/GSAP serão carregados lazy apenas onde usados.
 */
export function SmoothScrollProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
