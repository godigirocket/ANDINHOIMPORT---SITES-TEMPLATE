/**
 * NÃO UTILIZADO — dependia de @react-three/fiber e three, que não estão
 * instalados no projeto. O hero 3D atual usa CSS 3D transforms (mais leve
 * e sem risco de WebGL quebrar em dispositivos antigos), ver HeroSection.
 * Mantido apenas como referência; pode ser apagado com segurança.
 */
export {};
